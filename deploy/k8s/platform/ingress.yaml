apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubeclass-web
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubeclass-web
                port:
                  number: 80
